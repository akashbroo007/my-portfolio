export interface Project {
  name: string;
  description: string;
  tags: string[];
  link: string;
}

export interface TechTool {
  name: string;
  icon: string;
  level: string;
  description: string;
}